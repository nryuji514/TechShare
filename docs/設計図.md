# TechShare 設計図

技術ナレッジ共有アプリ「TechShare」の設計資料。
ER図 / 構成図 / フロー図 の3種類をまとめる。

> GitHub・VS Code（Markdown Preview Mermaid Support）でそのまま図として表示できる。
> 発表ではこの図をスクリーンショットしてスライドに貼る想定。

---

## 1. ER図（データベース設計）

ER図（テーブル構成とその関係）は、初心者向けの解説をつけた専用ファイルに一本化しました。
図の読み方（1対1 / 1対多 / 多対多、PK・FK・UK の意味）から、テーブルごとの説明まで載せています。

📄 **[ER図.md（やさしい版）はこちら](ER図.md)**

![ER図](diagrams/ER図.png)

> **色の意味**：🟦 ユーザー系（ユーザー／プロフィール）・🟩 コンテンツ系（記事／タグ）・🟥 反応系（コメント／いいね）

---

## 2. 構成図（システム構成・MVT）

DjangoのMVT（Model–View–Template）構成を表す。
ブラウザからのリクエストを `urls.py` が受け取り、対応する `views.py` の関数へ渡す。
ビューは `models.py`（DB）からデータを取り、`templates`（HTML）に渡して画面を作って返す。

![構成図](diagrams/構成図.png)

> **色の意味**：🟪 ユーザー・🟨 入口（URL）・🟩 処理の中心（View/Form）・🟦 データ（Model）・🟥 画面（Template）・⬜ DB

<details><summary>Mermaidソース（編集用）</summary>

```mermaid
%%{init: {'theme':'base','themeVariables':{'fontSize':'16px','fontFamily':'Segoe UI, Meiryo, sans-serif','lineColor':'#7a7a7a'},'flowchart':{'curve':'basis','nodeSpacing':45,'rankSpacing':60}}}%%
flowchart TD
    User["👤 ユーザー<br/>（ブラウザ）"]

    subgraph Django["🟩 Django アプリケーション（MVT）"]
        direction TB

        subgraph Routing["① 入口"]
            URLs["config/urls.py<br/>knowledge/urls.py<br/>（ルーティング）"]
        end

        subgraph Logic["② 処理の中心"]
            Views["knowledge/views.py<br/>（ビュー）"]
            Forms["knowledge/forms.py<br/>（フォーム / 入力チェック）"]
        end

        subgraph Data["③ データ（Model）"]
            Models["knowledge/models.py<br/>ユーザー / プロフィール / 記事<br/>タグ / コメント / いいね"]
        end

        subgraph View["④ 画面（Template）"]
            Templates["templates/*.html<br/>（base.html を継承）"]
            Static["static/css・画像<br/>media/（アップロード画像）"]
        end
    end

    DB[("🗄️ MySQL<br/>techshare DB")]

    User -- "① リクエスト（URL）" --> URLs
    URLs -- "② 振り分け" --> Views
    Views <-- "③ データ読み書き（ORM）" --> Models
    Models <-- "④ SQL" --> DB
    Views -- "⑤ フォーム処理" --> Forms
    Views -- "⑥ データを渡す" --> Templates
    Templates -- "⑦ CSS/画像を読込" --> Static
    Templates -- "⑧ HTMLを返す" --> User

    classDef user fill:#EDE7F6,stroke:#5E35B1,stroke-width:2px,color:#311B92;
    classDef routing fill:#FFF3CD,stroke:#E0A800,stroke-width:1.5px,color:#664D03;
    classDef logic fill:#D1E7DD,stroke:#0F5132,stroke-width:2px,color:#0F5132;
    classDef data fill:#CFE2FF,stroke:#084298,stroke-width:1.5px,color:#084298;
    classDef view fill:#F8D7DA,stroke:#842029,stroke-width:1.5px,color:#842029;
    classDef db fill:#E2E3E5,stroke:#41464B,stroke-width:2px,color:#1B1E21;

    class User user;
    class URLs routing;
    class Views,Forms logic;
    class Models data;
    class Templates,Static view;
    class DB db;
```

</details>

**ポイント**
- ビュー（`views.py`）が処理の中心。URL → ビュー → モデル/テンプレート、という流れ。
- DBアクセスは生のSQLではなくDjangoのORM（`Knowledge.objects.filter(...)` など）で書いている。
- アップロードされたプロフィール画像は `media/` に保存される（`settings.py` の `MEDIA_ROOT`）。

---

## 3. フロー図（画面遷移・ユーザーの動き）

ログインしてからアプリ内をどう移動するか（画面遷移）を表す。
`@login_required` がついているので、未ログインだとログイン画面に飛ばされる。

![フロー図](diagrams/フロー図.png)

> **色の意味**：🟪 入口・🟨 分岐／認証エリア・🟩 ホーム・🟦 記事系・🟥 マイページ系。点線の枠は「ログインしないと入れないエリア」。

<details><summary>Mermaidソース（編集用）</summary>

```mermaid
---
config:
  layout: elk
  theme: base
  themeVariables:
    fontFamily: "Segoe UI, Meiryo, sans-serif"
    fontSize: "16px"
    lineColor: "#7a7a7a"
  flowchart:
    curve: linear
---
flowchart TD
    Start([🚪 アクセス]) --> Login{ログイン済み？}

    subgraph Auth["🔑 認証エリア（未ログインでも入れる）"]
        direction LR
        LoginPage["ログイン画面<br/>/login/"]
        Register["新規登録<br/>/register/"]
        LoginPage -- "アカウント無し" --> Register
        Register -- "登録完了" --> LoginPage
    end

    Login -- "未ログイン" --> LoginPage
    Login -- "ログイン済み" --> Home
    LoginPage -- "ログイン成功" --> Home

    subgraph Member["🔒 ログイン後エリア（@login_required）"]
        direction TB
        Home["🏠 ホーム<br/>最新5件"]
        List["記事一覧<br/>/articles/<br/>検索・タグ絞り込み"]
        Detail["記事詳細<br/>/{pk}/"]
        Create["新規投稿<br/>/new/"]
        Edit["投稿編集<br/>/edit/{pk}/"]
        Mypage["マイページ<br/>/mypage/<br/>自分の投稿一覧"]
        ProfileEdit["プロフィール編集<br/>アイコン変更"]

        Home --> Create
        Home --> List
        Home --> Mypage
        Create -- "投稿後" --> List
        List --> Detail
        List -- "いいね♡・タグclick" --> List
        Detail -- "いいね♡・コメント投稿" --> Detail
        Detail -- "本人なら編集" --> Edit
        Edit -- "保存" --> Detail
        Mypage --> ProfileEdit
        ProfileEdit -- "保存" --> Mypage
        Mypage -- "編集" --> Edit
        Mypage -- "削除（確認）" --> Mypage
    end

    Home -- "ログアウト" --> LoginPage

    classDef startNode fill:#EDE7F6,stroke:#5E35B1,stroke-width:2px,color:#311B92;
    classDef decision fill:#FFF8E1,stroke:#F9A825,stroke-width:2px,color:#5D4037;
    classDef auth fill:#FFF3CD,stroke:#E0A800,stroke-width:1.5px,color:#664D03;
    classDef home fill:#D1E7DD,stroke:#0F5132,stroke-width:2px,color:#0F5132;
    classDef article fill:#CFE2FF,stroke:#084298,stroke-width:1.5px,color:#084298;
    classDef mypage fill:#F8D7DA,stroke:#842029,stroke-width:1.5px,color:#842029;

    class Start startNode;
    class Login decision;
    class LoginPage,Register auth;
    class Home home;
    class List,Detail,Create,Edit article;
    class Mypage,ProfileEdit mypage;
```

</details>

**ポイント**
- 「いいね」はどの画面（一覧・詳細）から押しても、押した画面にそのまま戻ってくる
  （ビュー側で `HTTP_REFERER` を見て戻り先を決めているため）。
- 削除はマイページから。`confirm()` で確認ダイアログを1回はさんでから消す。
- 投稿の編集は「本人だけ」可能（`get_object_or_404(..., author=request.user)` で制限）。

---

### 補足：いいね処理の流れ（toggle_like）

発表で聞かれやすい「いいね」の中身だけ、詳しめのフローにしておく。

![いいねフロー](diagrams/いいねフロー.png)

> **色の意味**：🟨 分岐・🟩 いいね追加・🟥 いいね取り消し。どちらに進んでも最後は同じ画面に戻る。

<details><summary>Mermaidソース（編集用）</summary>

```mermaid
%%{init: {'theme':'base','themeVariables':{'fontSize':'16px','fontFamily':'Segoe UI, Meiryo, sans-serif','lineColor':'#7a7a7a'},'flowchart':{'curve':'basis','nodeSpacing':45,'rankSpacing':55}}}%%
flowchart TD
    A([♡ ボタンを押す]) --> B["toggle_like ビュー<br/>POSTで呼ばれる"]
    B --> C{"すでに<br/>いいね済み？"}
    C -- "いいえ（新規）" --> D["Likeを1件作成<br/>get_or_create"]
    C -- "はい（既存）" --> E["そのLikeを削除<br/>＝ いいね取り消し"]
    D --> F["押した画面へ戻る<br/>（HTTP_REFERER）"]
    E --> F
    F --> G([最新のいいね数で再表示])

    classDef startNode fill:#EDE7F6,stroke:#5E35B1,stroke-width:2px,color:#311B92;
    classDef process fill:#CFE2FF,stroke:#084298,stroke-width:1.5px,color:#084298;
    classDef decision fill:#FFF8E1,stroke:#F9A825,stroke-width:2px,color:#5D4037;
    classDef add fill:#D1E7DD,stroke:#0F5132,stroke-width:2px,color:#0F5132;
    classDef remove fill:#F8D7DA,stroke:#842029,stroke-width:2px,color:#842029;

    class A,G startNode;
    class B,F process;
    class C decision;
    class D add;
    class E remove;
```

</details>
